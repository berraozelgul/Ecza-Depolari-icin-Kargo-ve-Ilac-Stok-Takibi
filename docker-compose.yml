version: '3.8'
services:
  backend:
    build: ./Backend
    container_name: kargotakip-backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env

  frontend:
    build: ./Frontend
    container_name: kargotakip-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend